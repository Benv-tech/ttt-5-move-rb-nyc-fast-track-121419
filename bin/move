#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Hereboard
board = [" ", " ", " ",]
def update_array_at_with(array, index, value)
  array[index] = value
end
update_array_at_with(board, 0, "X")
board
